import 'package:flutter/material.dart';

class CheaseScreen extends StatelessWidget {
  static const String routeName = 'cheaseScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
